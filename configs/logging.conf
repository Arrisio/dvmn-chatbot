[loggers]
keys = root

[handlers]
keys = consoleHandler, consoleError

[formatters]
keys = Formatter

[logger_root]
level = ERROR
handlers = consoleHandler

[handler_consoleHandler]
level = DEBUG
class = StreamHandler
args = (sys.stdout,)
formatter = Formatter

[handler_consoleError]
level = DEBUG
class = StreamHandler
args = (sys.stderr,)
formatter = Formatter

[formatter_Formatter]
format = %(asctime)s|%(levelname)-8s|%(name)-12s|%(process)d|%(message)s
